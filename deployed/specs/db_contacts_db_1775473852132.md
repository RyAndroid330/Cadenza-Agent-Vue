# Contacts DB (db)
Description: Stores contact records with fields: Name, Phone, Email, Address. Includes search index for query performance
Port: 4101

## Routes
- GET /contacts
- POST /contacts
- GET /contacts/:id
- PUT /contacts/:id
- DELETE /contacts/:id

## Data Stores
- contacts (array)