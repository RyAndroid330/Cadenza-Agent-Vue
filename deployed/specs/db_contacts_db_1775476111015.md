# Contacts DB (db)
Description: Stores contact records with phone numbers, emails, birthdays, and addresses
Port: 4107

## Routes
- GET /contacts
- POST /contacts
- GET /contacts/:id
- PUT /contacts/:id
- DELETE /contacts/:id

## Data Stores
- db (array)